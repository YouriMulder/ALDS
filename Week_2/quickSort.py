import random

class quickSort:


	def __init__(self, unsortedList):
		assert len(unsorted) > 0
		self.quickSortList = unsortedList

	def swap(self, firstIndex, secondIndex):
		temp = self.quickSortList[secondIndex]
		self.quickSortList[secondsIndex] = self.quickSortList[firstIndex]
		self.quickSortList[firstIndex] = temp


	def quickSort(self):
		random.seed()
		pivotIndex = random.randint(0, len(self.quickSortList))
		valueOfPivot = unsorted[pivotIndex]
		self.swapToLastIndex(pivotIndex, len(self.quickSortList) - 1)

		for j 


	
